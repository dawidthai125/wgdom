# NG-04.4 — Polish & EPIC Close · AUDIT

> **Status:** **AUDIT ONLY** — bez implementacji  
> **Data:** 2026-07-01  
> **Baseline prod:** **v2.63.11** · commit `adccb4e`  
> **Epic:** NG-04 — Kosztorys Workspace PRO · **faza zamykająca**  
> **Poprzednie fazy:** NG-04.1 (2.63.9) · NG-04.2 (2.63.10) · NG-04.3 (2.63.11)

**Cel:** Ocena spójności UX BOQ Explorer po NG-04.1–04.3 oraz lista drobnych poprawek przed **EPIC CLOSE** — **bez nowych funkcji**.

---

## 0. Werdykt audytu

| Obszar | Ocena | Uwagi |
|--------|-------|-------|
| Architektura (#001–#009) | **PASS** | ViewModel + derived caches — stabilne |
| UX spójność | **PARTIAL** | hierarchia nagłówków, duplikat CTA, etykiety kolumn |
| Mobile vs Desktop | **PARTIAL** | parity danych OK; touch targety ATH na kartach do dopracowania |
| Tooltipy ATH | **PASS** | deterministyczne copy; szum na `priced` |
| Benchmark | **PASS** | adapter pattern OK; etykieta mobile/desktop |
| Search / filtry | **PASS** | #003 zachowany; brak empty przy `rows=0` |
| Empty states | **PARTIAL** | search/filter OK; edge `rows=0` + brak BOQ skeleton |
| Loading states | **PARTIAL** | ProcessStatusBar OK; BOQ bez skeleton |
| Skeletony | **GAP** | brak w `kosztorys/*` |
| Spacing / hierarchy | **PARTIAL** | tytuł BOQ pod search; PRO hero vs BOQ |
| Accessibility | **PARTIAL** | search OK; tooltip/filter/table gaps |
| Wydajność | **PASS** | M8 + M8.10 + M8.11 green |
| Refactor (bez zmiany funkcji) | **LOW RISK** | hook cache, TooltipProvider, `BoqAthCell` |

**Rekomendacja:** **GO NG-04.4** — wyłącznie polish + docs + EPIC CLOSE. **Poza 04.4:** persist `code`, R/M/S inline, parser bump (G-02 / G-08).

---

## 1. Spójność UX BOQ Explorer

### 1.1 Mapa elementów (stan prod 2.63.11)

```text
TenderKosztorysWorkspace
├── KosztorysProcessStatusBar          [loading/process — OK]
├── TrustInlineHint
├── PRO hero KPI (4)                   [NG-04.0 reuse]
├── CTA: Pełny podgląd ATH | Pobierz   [workspace — OK]
├── KOSZTORYS PRO dashboard (4 KPI)
├── Ocena kosztorysu
├── TOP 20
└── KosztorysBoqExplorerSection
    ├── BoqAthSourceStrip              [NG-04.3 — chip + CTA]
    ├── sticky: search + filtry chips
    ├── (opcjonalnie) rows_fallback banner
    ├── h3 „BOQ Explorer”              [⚠ poniżej search — odwrócona hierarchia vs wireframe]
    ├── TenderMobileTableCards
    ├── TenderDesktopTable (10 kolumn)
    ├── Pokaż wszystkie (cap 20)
    ├── meta: N pozycji · filtr · search
    └── TrustReasonList
```

### 1.2 Niespójności UX (drobne poprawki)

| ID | Priorytet | Problem | Propozycja NG-04.4 |
|----|-----------|---------|-------------------|
| **UX-01** | P1 | Nagłówek „BOQ Explorer” **pod** search/filtrami; wireframe NG-04 miał tytuł + chip **nad** search | Przenieść `h3` + opcjonalny podtytuł **nad** strip/search; jeden blok nagłówka sekcji |
| **UX-02** | P2 | Duplikat CTA ATH: przycisk workspace + link w `BoqAthSourceStrip` | Zachować oba (#009), ale wizualnie: strip = link tekstowy secondary; workspace = primary button (już tak) — dodać `aria-describedby` lub krótki hint „ten sam podgląd” tylko w HelpView |
| **UX-03** | P1 | Kolumna desktop **„Benchmark”** vs mobile **„Benchmark rbh”** | Ujednolicić etykietę: **„Benchmark rbh”** wszędzie |
| **UX-04** | P2 | `rows_fallback` banner — w NG-04 freeze „dev-only”; nadal widoczny użytkownikowi | Ukryć za `import.meta.env.DEV` lub stonować do `text-[10px]` debug-only |
| **UX-05** | P2 | `sourceFilename` plain text gdy brak `athDocumentMeta` | OK jako fallback; upewnić się że nie duplikuje strip (już warunkowe) |
| **UX-06** | P2 | TrustReasonList **pod** tabelą — OK, ale poza sticky; przy długiej liście oddala od nagłówka | Bez zmian funkcji; opcjonalnie skrócić copy trust w BOQ (read-only) |

---

## 2. Mobile vs Desktop

| Aspekt | Desktop (`lg+`) | Mobile (`<lg`) | Verdict |
|--------|-----------------|-----------------|---------|
| Dane wiersza | 10-kolumnowa tabela | `TenderMobileRowCard` 2-col grid | **Parity OK** (#001 Many Views) |
| Search / filtry | sticky, full width | identyczne | **OK** |
| ATH tooltip | `min-h-0` na trigger | `min-h-[44px]` na trigger | **PARTIAL** — wysokie komórki w gridzie kart |
| Benchmark | compact badge | ten sam badge | **OK** |
| Horizontal scroll | `overflow-x-auto` | N/A (karty) | **OK** |
| Preview cap 20 | ten sam `PREVIEW_LIMIT` | ten sam | **OK** |

**Poprawki:**

| ID | Priorytet | Problem | Propozycja |
|----|-----------|---------|------------|
| **M-01** | P1 | `BoqAthTooltip` `min-h-[44px]` w kartach mobile rozciąga wiersze `dd` | Na mobile: target 44px tylko na ikonie `Info` (`p-2 -m-2`), nie na całym `span` z wartością |
| **M-02** | P2 | Brak `scope` / `caption` na tabeli desktop | `scope="col"` na `<th>`; `caption` sr-only „Tabela pozycji BOQ” |
| **M-03** | P2 | Filtry chips — brak `aria-pressed` | `aria-pressed={categoryFilter === opt.id}` na przyciskach filtra |

---

## 3. Spójność tooltipów ATH

| Stan | Copy (deterministyczne) | UI trigger | Verdict |
|------|-------------------------|------------|---------|
| `no_value_doc` | „W dokumencie znaleziono pozycję…” | dashed underline + ⓘ | **OK** |
| `no_match` | „Nie znaleziono odpowiadającej…” | dashed + ⓘ | **OK** |
| `priced` | „Cena została odczytana…” | ⓘ bez underline | **PARTIAL** — szum przy 500 wierszach |
| `empty_priced_row` | brak | plain value | **OK** |

**Poprawki:**

| ID | Priorytet | Problem | Propozycja |
|----|-----------|---------|------------|
| **ATH-01** | P1 | `priced` — ⓘ na **każdej** komórce Cena + Wartość ATH (2× ten sam tooltip) | Jedna ikona na wiersz (np. tylko Cena ATH) **lub** tooltip tylko na `no_value_doc` / `no_match` (freeze copy zostaje w lib; UI suppress na priced) |
| **ATH-02** | P1 | Trigger bez `aria-label` — ikona `aria-hidden` | `aria-label={tooltip}` na `TooltipTrigger` |
| **ATH-03** | P2 | Każdy `BoqAthTooltip` owija osobny `TooltipProvider` | Jeden `TooltipProvider` na poziomie sekcji BOQ |
| **ATH-04** | P2 | `BoqAthSourceStrip` filename chip — brak `aria-label` | `aria-label={`Plik źródłowy: ${meta.sourceFilename}`}` |

**Zakaz NG-04.4:** zmiana mapy stanów, parser, nowe pola ViewModel.

---

## 4. Spójność benchmarków

| Aspekt | Stan | Verdict |
|--------|------|---------|
| Cache | `buildBoqLaborBenchmarkCache` w sekcji | **OK** (#005–#006) |
| Adapter | `BoqLaborBenchmarkBadge` → `LaborBenchmarkStatusBadge compact` | **OK** |
| Brak benchmarku | `null` → pusta komórka | **PARTIAL** vs ATH „—” |
| Ceny tab | Osobny `LaborBenchmarkCell` — nie duplikuje BOQ | **OK** |

**Poprawki:**

| ID | Priorytet | Problem | Propozycja |
|----|-----------|---------|------------|
| **B-01** | P2 | Pusta komórka benchmark vs „—” w ATH | Ujednolicić empty: `—` w obu kolumnach prezentacji (bez zmiany logiki cache) |
| **B-02** | P1 | Etykieta kolumny (patrz UX-03) | „Benchmark rbh” |

---

## 5. Search i filtry

| Test | Wynik |
|------|-------|
| Search nie rebuilduje ViewModel | **PASS** (M8.2, #003) |
| Filtry branżowe pure `filterKosztorysBoqRows` | **PASS** |
| Reset `showAllRows` on search | **PASS** |
| Empty: search active | komunikat z query | **PASS** |
| Empty: category ≠ all | `kosztorysFilterEmptyMessage` | **PASS** |
| Empty: `filteredRows=0`, filter=all, no search | **null** (brak UI) | **GAP** |

**Poprawka UX-07 (P2):** Gdy `view.rows.length === 0` ale sekcja zamontowana — pokazać „Brak pozycji w katalogu” (spójne z V4 empty).

---

## 6. Empty states

| Scenariusz | Obecne zachowanie | Ocena |
|------------|-------------------|-------|
| Brak katalogu (`!pro.hasCatalog`) | `KosztorysEmptyMessage` / process | **OK** |
| Formal document | V4 display message | **OK** |
| Search 0 wyników | komunikat | **OK** |
| Filtr 0 wyników | komunikat branżowy | **OK** |
| Katalog pusty `rows=0` | strip + search, brak tabeli ani komunikatu | **GAP** |
| `inProgress` bez katalogu | `null` (cisza) | **OK** — ProcessStatusBar wystarcza |

---

## 7. Loading states

| Warstwa | Loading UX | Ocena |
|---------|------------|-------|
| Parse / dossier | `KosztorysProcessStatusBar` + retry | **OK** |
| ATH download | `Loader2` na przycisku | **OK** |
| BOQ ViewModel build | synchroniczny `useMemo` — brak osobnego stanu | **OK** (instant) |
| Tab mount lazy parse | brak wskaźnika w obszarze BOQ | **PARTIAL** |

**Uwaga:** NG-02 frozen — **nie** dodawać parse w BOQ. Ewentualny **placeholder** tylko gdy `pro.hasCatalog === false && inProgress` w workspace (już częściowo).

---

## 8. Skeletony

| Lokalizacja | Skeleton | Ocena |
|-------------|----------|-------|
| `kosztorys/*` | **brak** | **GAP** |
| Inne taby tender | różnie | poza scope |

**Rekomendacja NG-04.4 (P2, opcjonalne):** `BoqExplorerTableSkeleton` — 3–5 wierszy placeholder **tylko** gdy `phase.id` ∈ `parsing_kosztorys`, `preparing_docs` **i** `!pro.hasCatalog` — **bez** fałszywego skeletonu przy gotowych danych.

**Zakaz:** skeleton nie może sugerować danych ATH (zgodnie z #008).

---

## 9. Spacing i hierarchy

| Element | Issue | Fix |
|---------|-------|-----|
| PRO hero → dashboard → TOP 20 → BOQ | Długi scroll przed BOQ | **Akceptowalne** (decision screen); bez zmiany kolejności w 04.4 |
| BOQ section `space-y-3` | Spójne z workspace | **OK** |
| Sticky search `-mx-1` | Lekki bleed | **OK** |
| Meta footer `text-[10px]` | Czytelne | **OK** |
| UX-01 | Tytuł sekcji | Patrz §1.2 |

---

## 10. Accessibility

| Kontrolka | Stan | Gap |
|-----------|------|-----|
| Search input | `aria-label` | **OK** |
| Filtry chips | brak `aria-pressed` | **M-03** |
| ATH tooltip | brak accessible name | **ATH-02** |
| Benchmark badge | ikona `aria-hidden`, tekst widoczny w compact | **OK** |
| Tabela | brak `caption`, `scope` | **M-02** |
| „Pokaż wszystkie” | button native | **OK** |
| Keyboard | Radix tooltip focusable | **PARTIAL** — provider nesting |
| `Show all` / search | brak live region dla count | **P3** — `aria-live="polite"` na meta „N pozycji” |

---

## 11. Wydajność (NG-04.1–04.3)

| Metryka | Próg | Wynik (M8 / ng04-3) |
|---------|------|---------------------|
| `buildKosztorysBoqExplorerView(500)` | < 2500 ms | ~50 ms |
| 50× search filter | < 600 ms | ~3 ms |
| `buildBoqLaborBenchmarkCache(500)` | < 50 ms | ~0.7 ms |
| `buildBoqAthPresentationCache(500)` | < 50 ms | ~0.3 ms |
| Cache ref po filter | stable | **PASS** |

**Wniosek:** Brak regresji wydajnościowej — NG-04.4 **nie** wymaga optymalizacji algorytmów.

**Micro (P3):** `TooltipProvider` per cell — koszt React tree; refaktor ATH-03 bez zmiany UX.

---

## 12. Kod — uproszczenia (bez zmiany funkcji)

| ID | Plik | Propozycja | Ryzyko |
|----|------|------------|--------|
| **R-01** | `KosztorysBoqExplorerSection` | `useBoqDerivedCaches(item, view.rows)` — jeden hook dla benchmark + ATH | LOW |
| **R-02** | `KosztorysBoqRowFields` | `BoqAthCell` komponent zamiast inline `athCell()` | LOW |
| **R-03** | `BoqAthTooltip` | Props `showTrigger?: boolean` dla suppress priced | LOW |
| **R-04** | `KosztorysBoqExplorerSection` | `BoqExplorerSectionHeader` — title + strip + search | LOW |
| **R-05** | lib | `BOQ_PREVIEW_LIMIT = 20` export SSOT | LOW |

**Zakaz refaktoru:** merge explorer, parsery, ViewModel shape.

---

## 13. Dokumentacja / HelpView (EPIC gap)

| Dokument | Stan |
|----------|------|
| `GuideView.tsx` FAQ Kosztorys PRO | **STALE** — opisuje „pełna tabela ATH pod spodem”, brak BOQ Explorer, benchmark, tooltipów |
| `NG-04-DESIGN-FREEZE.md` | Fazy 04.1–04.3 nie oznaczone CLOSED w nagłówku |
| `CURRENT-TASK.md` | NG-04.4 ACTIVE — **OK** |

**DOC-01 (P1):** Zaktualizować HelpView + changelog hint dla BOQ Explorer / ATH explain / benchmark rbh.

---

## 14. Lista drobnych poprawek (backlog NG-04.4)

### P1 — przed EPIC CLOSE

| ID | Opis |
|----|------|
| UX-01 | Hierarchia nagłówka BOQ (tytuł nad search) |
| UX-03 / B-02 | Etykieta „Benchmark rbh” desktop + mobile |
| ATH-01 | Redukcja szumu tooltip `priced` |
| ATH-02 | `aria-label` na triggerach ATH |
| M-01 | Touch target mobile bez rozciągania wartości |
| DOC-01 | HelpView / FAQ sync z NG-04 |

### P2 — polish w tej samej fazie

| ID | Opis |
|----|------|
| UX-04 | `rows_fallback` dev-only |
| UX-07 | Empty state `rows=0` |
| ATH-03 | Wspólny TooltipProvider w sekcji |
| ATH-04 | aria filename chip |
| M-02 | `caption` + `scope` tabela |
| M-03 | `aria-pressed` filtry |
| B-01 | Empty benchmark → „—” |

### P3 — opcjonalne / jeśli czas

| ID | Opis |
|----|------|
| Skeleton | `BoqExplorerTableSkeleton` przy parse in progress |
| aria-live | meta count po filtrze |
| R-01–R-05 | refaktor strukturalny |

### Poza NG-04.4 (explicit)

- Persist `code` w snapshot (G-08)
- Inline R/M/S (G-02)
- Filtr „tylko below benchmark”
- Virtualizacja 500+ wierszy
- Zmiany NG-02 runtime

---

## 15. Analiza regresji (NG-04.4)

| Obszar | Ryzyko | Mitigacja |
|--------|--------|-----------|
| ViewModel / merge | **NONE** | zakaz dotyku explorer merge |
| Derived caches | **LOW** | tylko prezentacja; pełna regresja ng04-2/3 |
| Tooltip copy | **LOW** | ATH-01 to UI-only suppress |
| Benchmark logika | **NONE** | B-01 to display only |
| Mobile layout | **LOW** | M-01 manual smoke ≤390px |
| HelpView | **NONE** | docs only |
| Wydajność | **NONE** | brak nowych cache passes |
| TP200B / process | **NONE** | frozen paths |

**Regression gate (obowiązkowy):**

```bash
npx vite-node scripts/test-ng04-3-ath-fidelity.mjs
npx vite-node scripts/test-ng04-2-benchmark-per-line.mjs
npx vite-node scripts/test-ng04-kosztorys-boq-explorer.mjs
npx vite-node scripts/test-ng04-m8-large-boq-performance.mjs
npx vite-node scripts/test-v41-kosztorys-workspace.mjs
npx vite-node scripts/test-tender-kosztorys-process-phase.mjs
npx vite-node scripts/test-tp200b-snapshot-fidelity.mjs
npm run build
```

---

## 16. Akceptacja audytu

- [x] Przegląd BOQ Explorer 2.63.11 zakończony
- [x] Lista poprawek P1–P3 gotowa
- [x] Analiza regresji gotowa
- [ ] DESIGN FREEZE NG-04.4 — [`docs/NG-04.4-DESIGN-FREEZE.md`](../docs/NG-04.4-DESIGN-FREEZE.md)
- [ ] Plan EPIC CLOSE — [`docs/NG-04-EPIC-CLOSE-PLAN.md`](../docs/NG-04-EPIC-CLOSE-PLAN.md)
- [ ] **GO PLAN** (na polecenie)

**Prognoza wersji:** **2.63.12**
